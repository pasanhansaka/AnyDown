# backend/nixpacks.toml
[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = ["npm install", "curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp", "chmod +x /usr/local/bin/yt-dlp"]

[start]
cmd = "npm start"
